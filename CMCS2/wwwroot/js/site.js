// Please see documentation at https://learn.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

/*<script>
    document.getElementById('HoursWorked').addEventListener('input', calculateTotalAmount);
    document.getElementById('HourlyRate').addEventListener('input', calculateTotalAmount);

    function calculateTotalAmount() {
        const hoursWorked = parseFloat(document.getElementById('HoursWorked').value);
    const hourlyRate = parseFloat(document.getElementById('HourlyRate').value);

    if (!isNaN(hoursWorked) && !isNaN(hourlyRate)) {
            const totalAmount = (hoursWorked * hourlyRate).toFixed(2);
    document.getElementById('TotalAmount').value = `$${totalAmount}`;
        } else {
        document.getElementById('TotalAmount').value = "(Auto-calculated)";
        }
    }
</script>*/
